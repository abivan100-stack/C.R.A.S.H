# local server / logs
*.log
server.log

# python
__pycache__/
*.pyc

# secrets + virtualenvs (never commit the MongoDB URI — it lives only in .env)
.env
.env.local
.env.*.local
.venv/
venv/

# OS junk
.DS_Store
Thumbs.db
